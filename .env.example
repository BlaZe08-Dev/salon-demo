# Optional. Lets `npm run fetch-google` / `npm run build` pull the salon's name, address,
# phone, hours, rating, reviews and photos from Google (Places API (New)).
# Copy this file to .env for local use, or set it in your hosting platform's environment settings.
GOOGLE_PLACES_API_KEY=
